# Frontend

Thư mục dành cho ứng dụng giao diện của hệ thống quản lý đồ án tốt nghiệp.

Backend API nằm trong `../backend` và mặc định chạy tại `http://localhost:8080`.

Khi khởi tạo frontend, cấu hình URL API qua biến môi trường thay vì ghi cứng trong mã nguồn.
